prefire multiplexer to check if any has hit its threshold like heat matrix smoke...

create a new class call it sensor_fuser to calculate/set thresholds

create a datasheet.txt that we can have like the config file we did for amazing and use its values to validate
this will make it easier to map this structure to any hardware we want we can easily change thresholds
we can also add extra features or choose what features we want to run
